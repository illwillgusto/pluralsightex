// Using conditionals
let badgeColor;

if (numYearsService < 5) {
  badgeColor = "blue";
} else if (numYearsService < 10) {
  badgeColor = "yellow";
} else if (numYearsService < 15) {
  badgeColor = "black";
} else if (numYearsService < 20) {
  badgeColor = "silver";
} else {
  badgeColor = "gold";
}
